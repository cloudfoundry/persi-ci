---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: google/cloud-sdk
    tag: 229.0.0-slim

inputs:
- name: persi-ci

params:
  FILESTORE_INSTANCE_NAME:
  GCP_LOCATION:
  GCP_SERVICE_ACCOUNT_KEY:

run:
  path: persi-ci/scripts/ci/delete-gcp-nfs-volume.sh
