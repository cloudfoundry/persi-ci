platform: linux

image_resource:
  type: docker-image
  source:
    repository: cryogenics/essentials

inputs:
- name: pooled-env

run:
  path: /bin/sh
  args:
    - "-c"
    - |
      set -euo pipefail
      jq . pooled-env/metadata
