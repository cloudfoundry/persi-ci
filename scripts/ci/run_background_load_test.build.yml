---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/cf-deployment-concourse-tasks
    tag: latest

inputs:
  - name: persi-ci
  - name: cf-volume-services-acceptance-tests
  - name: bbl-state

params:
  BBL_STATE_DIR:
  CF_USERNAME:
  CF_API_ENDPOINT:
  APPS_DOMAIN:
  SERVICE_BROKER_NAME: nfsbroker
  SERVICE_NAME: nfs
  PLAN_NAME: Existing
  CREATE_CONFIG:
  BIND_CONFIG:

run:
  path: persi-ci/scripts/ci/run_background_load_test
