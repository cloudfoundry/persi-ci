---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: harbor-repo.vmware.com/cryogenics/essentials
    tag: latest

inputs:
- name: distributed-package
- name: bosh-release
- name: task-repo
- name: persi-ci

outputs:
- name: bosh-release-updated

params:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  AWS_ROLE_ARN:
  GIT_USERNAME:
  GIT_EMAIL:
  GIT_BRANCH:
  BLOB_NAME:
  BLOB_EXTENSION: tar.xz

run:
  path: persi-ci/scripts/ci/bump-ftp-blob
