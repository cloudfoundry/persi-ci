- path: /instance_groups/name=openvpn/networks/name=default
  type: replace
  value:
    name: openvpn
    default:
    - dns
    - gateway

- path: /instance_groups/name=openvpn/networks/-
  type: replace
  value:
    name: vip
    static_ips:
    - ((wan_ip))

